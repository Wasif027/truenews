# Base URL of the TrueNews API. Used both server-side and in the browser.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Public URL of the deployed site (for OG tags / metadataBase). Optional locally.
NEXT_PUBLIC_SITE_URL=
